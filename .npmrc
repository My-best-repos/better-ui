# ==============================================================================
# .NPMRC CONFIGURATION
# ==============================================================================

# Validates local Node.js version against package.json engines.
engine-strict=true

# pnpm: automatically install peerDependencies if missing.
auto-install-peers=true

# pnpm: relax peer dep version mismatches (warns instead of fails).
strict-peer-dependencies=false
